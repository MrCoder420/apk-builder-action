name: React Native APK Build
on:
  workflow_dispatch:
    inputs:
      repo_url:
        description: 'Git repository URL'
        required: true
        default: 'https://github.com/priyanshusharma99/SoulShuffle'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Clone Target Repo
        run: |
          git clone ${{ github.event.inputs.repo_url }} .
          ls -F # This lets us see the folder structure in the logs

      - name: Install Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install NPM Dependencies
        run: npm install --legacy-peer-deps

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Build Android Release
        run: |
          # We use a wildcard check to find the android folder just in case
          cd **/android || cd android
          chmod +x gradlew
          ./gradlew assembleRelease

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: soulshuffle-apk
          path: "**/android/app/build/outputs/apk/release/*.apk"
